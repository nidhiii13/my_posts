const routes = {
  REGISTER: "/register",
  LOGIN: "/login",
  POSTS: "/posts",
  POSTSBYID : "/:userId",
  NEWPOST : "/new",
  USERNAME: "/:username",
  USERID: "/:userId",
  NEW: "/new",
};

module.exports = routes;
